# 워크북 학습 위치 복귀 QA

- 워크북 경로의 `lesson` 쿼리 값으로 현재 레슨을 초기화해, `/workbook/high_univ/1?lesson=1`에서 두 번째 레슨의 기본 교재 내용이 표시되는 것을 확인했다.
- AI 학습 가이드에서 전달하는 `return_to` 값이 있으면 주제 설정과 주제문 점검 화면 상단에 **학습 중인 레슨으로 돌아가기** 버튼이 표시된다.
- `return_to`는 동일 서비스의 허용된 워크북 경로만 인정하며, 외부 URL·잘못된 과정명·비정상 레슨 값은 무시한다.
