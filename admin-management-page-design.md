# 논술 마스터 관리자 대시보드 상세 페이지 설계안

## 1. 문서 목적

본 문서는 **논술 마스터(Essay Master)**의 관리자 대시보드에서 수료증을 안전하게 운영하고 사이트 전체 학습 카테고리와 기능을 통제하기 위한 상세 페이지 설계안이다. 현재 프로젝트의 관리자 권한, 학생 상세 페이지, 수료증 센터, AI 학습도구 및 교사 첨삭 흐름을 기준으로 작성했으며, 화면 설계뿐 아니라 데이터 모델, API 계약, 권한 정책, 검증 기준 및 단계적 적용 방법까지 포함한다.

핵심 목표는 다음 세 가지이다. 첫째, 관리자가 학생별 수료증을 발행·발행취소·삭제할 수 있되, 실수나 중복 발급을 방지한다. 둘째, 초등·중고등 커리큘럼과 AI 첨삭·교사 첨삭 같은 학습 영역을 관리자 화면에서 일관되게 활성화하거나 제한한다. 셋째, 모든 중요한 운영 행위를 기록하여 나중에 누가 언제 어떤 변경을 수행했는지 추적할 수 있도록 한다.

> **중요한 설계 원칙:** 화면의 ON/OFF 상태만 변경하는 것이 아니라, 서버 권한 검사와 데이터베이스의 변경 이력까지 함께 구현해야 실제 운영 가능한 관리자 기능이 된다.

---

## 2. 현재 구조를 기준으로 한 설계 전제

현재 서비스에는 `admin`과 일반 학습자 중심의 역할 체계가 있고, `/admin` 관리자 대시보드와 `/admin/student/:id` 학생 상세 페이지가 운영되고 있다. 수료증 데이터는 `certificate` 테이블에 과정 유형, 레벨, 수료증 유형, 공유 토큰, PDF URL, 발행 시각을 저장하는 구조를 사용한다. 다만 발행취소 상태를 별도로 저장하는 필드는 없으므로, 운영 기능을 완성하려면 단순 삭제와 발행취소를 분리할 수 있는 상태 모델이 필요하다.

현재 카테고리 활성화 상태가 프론트엔드 임시 상태로만 관리된다면 새로고침이나 서버 인스턴스 변경 후 값이 유지되지 않을 수 있다. 따라서 카테고리 통제는 별도의 설정 테이블에 저장하고, 학습자용 API에서도 비활성화된 기능을 실제로 차단해야 한다.

| 구분 | 현재 기준 | 설계 방향 |
|---|---|---|
| 관리자 인증 | `user.role === "admin"` | 모든 관리자 API에서 서버 측 재검사 |
| 학생 관리 | `/admin`, `/admin/student/:id` | 통합 운영 대시보드 안에 영역별 탭 제공 |
| 수료증 | 과정, 레벨, 유형, 공유 토큰, 발행일 | 발행·활성·취소·삭제 상태와 감사 로그 추가 |
| 카테고리 | 초등·중고등 및 학습도구 | 영속화된 기능 플래그와 적용 범위 추가 |
| 교사 첨삭 | 학생 제출물별 피드백 흐름 | 교사 역할 및 배정 현황을 별도 운영 탭에서 관리 |
| AI 학습도구 | 공용 AI 사용량·일일 쿼터 | 사용량, 장애 상태, 기능별 ON/OFF를 통합 관리 |

---

## 3. 관리자 정보구조(IA)

관리자 메뉴는 일반 학습자 메뉴와 분리하고, 관리자 프로필 메뉴 또는 `/admin` 진입점에서만 접근하도록 한다. 화면 상단에는 현재 운영 범위를 명확히 보여주는 브레드크럼을 둔다.

```text
관리자 대시보드
├── 운영 개요
├── 학습자 관리
├── 수료증 관리
│   ├── 전체 발급 목록
│   ├── 학생별 수료증
│   ├── 발급 대기/검토
│   └── 취소·삭제 이력
├── 카테고리 및 기능 통제
│   ├── 커리큘럼 카테고리
│   ├── AI 학습도구
│   ├── 교사 첨삭지도
│   └── 오프라인/PWA 기능
├── 교사 첨삭 운영
├── AI 학습도구 운영
├── 감사 로그
└── 관리자 설정
```

초기 구현에서는 별도 URL을 너무 많이 만들기보다 `/admin` 내부의 탭과 하위 라우트를 함께 사용하는 방식을 권장한다. 예를 들어 `/admin?tab=certificates` 또는 `/admin/certificates`를 사용할 수 있다. 운영 규모가 커지면 수료증과 카테고리 설정을 별도 페이지로 분리한다.

---

## 4. 관리자 대시보드 공통 레이아웃

### 4.1 상단 헤더

헤더에는 서비스명, 현재 페이지명, 관리자 계정, 마지막 동기화 시각, 알림 아이콘을 배치한다. 중요한 변경이 저장되었을 때는 성공 토스트를 표시하되, 발행취소·삭제처럼 되돌리기 어려운 작업에는 토스트만 사용하지 않고 확인 모달을 병행한다.

### 4.2 운영 요약 카드

수료증과 카테고리 관리의 상태를 한눈에 확인할 수 있도록 다음 카드를 제공한다.

| 카드 | 표시 값 | 상호작용 |
|---|---:|---|
| 전체 발급 수료증 | 전체 활성·취소 수료증 수 | 수료증 목록으로 이동 |
| 이번 달 발급 | 최근 30일 발급 건수 | 기간 필터 적용 |
| 발행취소 대기/최근 취소 | 취소 건수 또는 최근 취소 수 | 취소 이력으로 이동 |
| 활성 학습 카테고리 | ON 상태 기능 수 / 전체 수 | 카테고리 통제 화면으로 이동 |
| AI 오늘 호출량 | 오늘 호출 수 / 허용량 | AI 운영 화면으로 이동 |
| 미처리 교사 첨삭 | 검토 대기 제출물 수 | 교사 운영 화면으로 이동 |

### 4.3 공통 필터 및 검색

학생 이름, 이메일, 사용자 ID, 과정, 레벨, 수료증 상태, 발행일 범위로 검색할 수 있게 한다. 필터 적용 상태는 URL query 또는 저장 가능한 화면 상태로 관리하여 새로고침 후에도 유지할 수 있도록 한다.

---

## 5. 수료증 관리 페이지 설계

### 5.1 전체 수료증 목록

경로는 `/admin/certificates`를 권장한다. 목록의 기본 정렬은 최근 발행일 내림차순이며, 학생명·과정·레벨·상태·발행일·관리자 기준으로 정렬할 수 있다.

| 컬럼 | 설명 |
|---|---|
| 선택 | 여러 건을 선택하여 CSV 내보내기 또는 이력 확인 |
| 학생 | 학생 이름, 이메일, 사용자 ID |
| 과정 | 초등 논술 또는 중고등 논술 |
| 레벨 | Level 1~4 또는 졸업 과정 |
| 수료증 유형 | 레벨 수료증 또는 졸업증서 |
| 상태 | 활성, 발행취소, 삭제됨 |
| 발행일 | 로컬 시간대 기준 표시, 서버는 UTC 저장 |
| 공유 | 공개 링크 복사 또는 미리보기 |
| 관리 | 상세, 발행취소, 삭제 메뉴 |

상태가 `발행취소`인 수료증은 학생용 수료증 센터와 공유 링크에서 더 이상 유효한 증서로 취급하지 않는다. 단, 운영 기록을 보존하기 위해 데이터베이스 행 자체는 유지하는 것을 기본 정책으로 한다.

### 5.2 학생별 수료증 관리

학생 상세 페이지 `/admin/student/:id`의 수료증 섹션에는 다음 기능을 추가한다.

1. 현재 보유 중인 활성 수료증 목록을 표시한다.
2. 취소된 수료증이 있으면 별도 접기 영역에서 이력을 표시한다.
3. `수료증 발행` 버튼을 누르면 과정, 레벨, 유형, 발행 사유를 입력하는 모달을 연다.
4. 동일 학생·과정·레벨·유형의 활성 수료증이 이미 있으면 발행 버튼을 차단한다.
5. `발행취소`를 누르면 사유를 필수 입력으로 요구한다.
6. `삭제`는 기본 목록에서 숨기는 작업이 아니라, 법적·운영적 필요가 있을 때만 수행하는 강한 작업으로 분리한다.

### 5.3 발행 모달

발행 모달의 필드는 다음과 같이 설계한다.

| 필드 | 필수 여부 | 검증 |
|---|---|---|
| 학생 | 필수 | 현재 학생 ID와 일치하는지 서버 검증 |
| 과정 | 필수 | `elementary` 또는 `middle_high` |
| 레벨 | 레벨 수료증이면 필수 | 1~4 범위 |
| 수료증 유형 | 필수 | `level_certificate` 또는 `graduation_certificate` |
| 발행 사유 | 권장 | 최소 5자 이상 |
| 미리보기 | 선택 | 발행 전 이름·과정·레벨·발행일 확인 |

발행 확정 전에는 중복 검사 결과를 모달 안에 표시한다. 발행 성공 후에는 발급번호 또는 공유 토큰을 표시하고, 수료증 미리보기·링크 복사·학생 상세로 돌아가기 버튼을 제공한다.

### 5.4 발행취소 및 삭제 모달

발행취소는 소프트 삭제이다. 즉 `status = revoked`, `revokedAt`, `revokedBy`, `revocationReason`을 기록하고 공개 링크의 유효성을 차단한다. 삭제는 별도 권한을 요구하는 하드 삭제 또는 개인정보 삭제 정책에 따른 제한적 삭제로 분리한다.

확인 모달에는 다음 문구를 명확히 표시해야 한다.

> 이 수료증을 발행취소하면 학생용 수료증 목록과 공유 링크에서 유효한 증서로 표시되지 않습니다. 발행취소 사유는 관리자 감사 로그에 기록됩니다.

삭제 버튼은 기본적으로 비활성화하거나 더보기 메뉴 안에 배치한다. 운영상 삭제가 필요한 경우 관리자 비밀번호 재확인 또는 2차 인증을 요구하는 확장 정책을 적용할 수 있다.

### 5.5 권장 데이터 모델

기존 `certificate` 테이블을 확장할 때 다음 필드를 권장한다.

| 필드 | 타입 예시 | 목적 |
|---|---|---|
| `status` | `active \| revoked` | 현재 유효 상태 |
| `issuedBy` | integer | 발행 관리자 ID |
| `revokedBy` | integer nullable | 취소 관리자 ID |
| `revokedAt` | timestamp nullable | 취소 시각 |
| `revocationReason` | text nullable | 취소 사유 |
| `issueReason` | text nullable | 발행 사유 |
| `updatedAt` | timestamp | 마지막 변경 시각 |

삭제 작업은 `certificate_audit_log`에 기록한 후 실제 행 삭제 여부를 정책으로 결정한다. 추천 방식은 수료증 본체는 보존하고 감사 로그와 상태 필드로 관리하는 것이다.

---

## 6. 카테고리 및 기능 통제 페이지 설계

경로는 `/admin/categories` 또는 관리자 대시보드의 `카테고리 및 기능 통제` 탭을 권장한다. 화면은 **카테고리 그룹**, **기능 플래그**, **적용 대상**, **현재 상태**, **변경 이력**을 중심으로 구성한다.

### 6.1 카테고리 그룹

| 그룹 | 세부 기능 | 기본 상태 |
|---|---|---|
| 초등 논술 | 초등 커리큘럼, 워크북, 퀴즈 | ON |
| 중고등 논술 | 중고등 커리큘럼, 요약, 주제 설정 | ON |
| 교사 첨삭 | 제출물 배정, 점수, 코멘트, 줄 단위 피드백 | ON |
| AI 학습도구 | AI 첨삭, 문장 교정 퀴즈, 요약 연습 | ON |
| 오프라인/PWA | 오프라인 보관함, 자동 동기화 | ON |
| 수료증 | 발급 요청, 학생 조회, 공유 링크 | ON |

### 6.2 기능 통제 카드

각 기능은 카드 또는 테이블 행으로 표시한다. 카드에는 기능명, 설명, 현재 상태, 최근 변경자, 최근 변경 시각, 적용 범위, 변경 버튼을 포함한다.

| UI 항목 | 예시 |
|---|---|
| 상태 스위치 | 활성화됨 / 차단됨 |
| 적용 범위 | 전체 사용자, 학습자만, 교사만 |
| 차단 안내 문구 | 학생에게 표시할 안내 문구 |
| 예약 변경 여부 | 즉시 적용 또는 예약 적용 확장 |
| 최근 변경 | 관리자 이름과 시각 |

기능을 OFF로 변경하면 확인 모달에서 영향 범위를 보여준다. 예를 들어 AI 자동 첨삭을 차단하면 학생의 AI 첨삭 요청 API가 서버에서 거부되고, 기존 저장된 피드백은 열람 가능하도록 할지 여부를 함께 확인한다.

### 6.3 서버 적용 원칙

프론트엔드에서 버튼을 숨기는 것만으로는 통제권한이 완성되지 않는다. 다음 두 위치에서 같은 설정을 적용해야 한다.

1. 관리자 API: 관리자만 기능 설정을 변경할 수 있도록 `adminProcedure`에서 권한을 검사한다.
2. 학습 API: 기능이 OFF일 때 학생 요청을 서버에서 차단하고 안내 메시지를 반환한다.

예를 들어 AI 첨삭이 비활성화된 경우 `aiFeedback.create` 또는 해당 기능의 tRPC mutation이 `FORBIDDEN` 또는 명확한 비즈니스 오류를 반환해야 한다. 학생 페이지는 이 응답을 받아 “현재 관리자 설정으로 일시 중지된 기능입니다”라는 안내를 표시한다.

### 6.4 권장 설정 데이터 모델

`platform_feature_flags` 테이블을 별도로 만들고 다음 필드를 저장한다.

| 필드 | 설명 |
|---|---|
| `key` | `elementary_curriculum`, `ai_feedback` 등 고유 키 |
| `enabled` | 활성화 여부 |
| `audience` | `all`, `student`, `teacher`, `admin` |
| `displayMessage` | 비활성화 시 사용자 안내 문구 |
| `updatedBy` | 마지막 변경 관리자 ID |
| `updatedAt` | 마지막 변경 시각 |
| `version` | 동시 수정 충돌 방지용 버전 |

관리자가 변경할 때는 `platform_feature_flag_audit`에 이전 값, 새 값, 사유, 관리자 ID, IP 또는 세션 식별자를 기록한다.

---

## 7. 교사 첨삭 및 AI 학습도구 운영 대시보드 연결

수료증과 카테고리 통제 화면만 분리하면 운영자가 서비스 상태를 판단하기 어렵기 때문에, 각 영역의 핵심 운영지표를 같은 관리자 IA 안에 연결한다.

### 7.1 교사 첨삭 운영 화면

교사별 배정 제출물, 미처리 제출물, 평균 처리 시간, 최근 피드백 점수, 학생별 피드백 이력을 표시한다. 초기 단계에서는 교사 역할이 별도로 없을 경우 관리자 계정을 교사 대행 계정으로 표시하되, 향후 `teacher` 역할을 추가할 수 있도록 권한 모델을 확장한다.

### 7.2 AI 학습도구 운영 화면

AI 호출 총량, 오늘 호출량, 기능별 호출량, 학생별 쿼터 소진 수, 오류율, 최근 실패 요청을 표시한다. 학생 개인정보와 논술 원문은 운영 요약 화면에 불필요하게 노출하지 않고, 필요할 때만 권한이 있는 상세 화면에서 열람하도록 한다.

| 지표 | 용도 |
|---|---|
| 오늘 AI 호출 | 공용 크레딧 사용량 확인 |
| 기능별 호출 비중 | 어떤 학습도구가 많이 사용되는지 확인 |
| 쿼터 소진 학생 수 | Pro Pass 또는 추가 크레딧 안내 대상 파악 |
| 평균 응답 시간 | AI 서비스 품질 모니터링 |
| 실패율 | API 키·프록시·네트워크 문제 조기 탐지 |

---

## 8. 권한 정책

최소 권한 원칙을 적용한다. 현재 관리자 역할이 하나뿐이라도 API 레벨에서는 작업별 권한을 분리할 수 있는 구조로 설계한다.

| 권한 | 조회 | 발행 | 발행취소 | 삭제 | 카테고리 변경 | 감사 로그 |
|---|---:|---:|---:|---:|---:|---:|
| 최고 관리자 | 가능 | 가능 | 가능 | 가능 | 가능 | 가능 |
| 운영 관리자 | 가능 | 가능 | 가능 | 제한 | 가능 | 가능 |
| 교사 관리자 | 학생 학습정보 제한 조회 | 불가 또는 제한 | 불가 | 불가 | 불가 | 본인 작업만 |
| 일반 학습자 | 본인 수료증만 | 정책에 따른 요청 | 불가 | 불가 | 불가 | 불가 |

모든 관리자 mutation은 `ctx.user.role`을 확인해야 하며, 학생 ID나 수료증 ID가 존재하지 않거나 다른 리소스에 속해 있으면 작업을 거부해야 한다.

---

## 9. tRPC API 설계안

```text
admin.certificates.list({ search, courseType, level, status, dateFrom, dateTo })
admin.certificates.getByStudent({ studentId })
admin.certificates.issue({ studentId, courseType, level, certificateType, reason })
admin.certificates.revoke({ certificateId, reason })
admin.certificates.delete({ certificateId, reason })
admin.certificates.preview({ studentId, courseType, level, certificateType })

admin.features.list()
admin.features.update({ key, enabled, audience, displayMessage, reason })
admin.features.bulkUpdate({ changes, reason })
admin.features.audit({ key, dateFrom, dateTo })

admin.operations.overview()
admin.operations.teacherStats({ teacherId, dateFrom, dateTo })
admin.operations.aiStats({ dateFrom, dateTo, actionType })
```

수료증 발행과 발행취소는 성공 여부만 반환하지 말고 변경된 레코드와 감사 로그 ID를 함께 반환하는 것이 좋다. 이렇게 하면 UI에서 즉시 목록을 갱신하고 운영자가 작업 결과를 추적할 수 있다.

---

## 10. 사용자 경험 및 접근성 기준

관리자 화면은 데스크톱 우선이지만 태블릿과 모바일에서도 주요 작업이 가능해야 한다. 테이블은 작은 화면에서 가로 스크롤을 허용하고, 수료증 상세 작업은 카드형 레이아웃으로 전환한다.

모든 아이콘 버튼에는 `aria-label`을 제공하고, 수료증 발행취소·삭제 같은 작업은 키보드로 접근 가능한 모달과 명확한 제목을 사용한다. Dialog에는 반드시 접근성용 `DialogTitle`을 포함하며, 색상만으로 상태를 구분하지 않고 텍스트 라벨과 아이콘을 함께 사용한다.

변경 완료 후에는 성공 토스트를 보여주고, 실패 시 서버 오류를 그대로 노출하지 말고 관리자에게 이해 가능한 메시지로 변환한다. 작업 중에는 버튼을 비활성화하여 중복 제출을 방지한다.

---

## 11. 검증 및 테스트 계획

### 11.1 단위 테스트

수료증 발행 시 동일 학생·과정·레벨·유형의 중복 발행이 차단되는지 검증한다. 발행취소 후 학생용 조회에서 유효 수료증으로 반환되지 않는지 확인한다. 일반 학습자가 관리자 mutation을 호출할 때 `FORBIDDEN`이 반환되는지도 필수 테스트 항목이다.

카테고리 기능을 OFF로 변경한 뒤 학습자용 AI 첨삭·교사 첨삭·커리큘럼 API가 차단되는지 확인한다. 설정 변경 이력에 관리자 ID, 이전 값, 새 값, 사유가 남는지 테스트한다.

### 11.2 통합 테스트

관리자 로그인 → 수료증 목록 조회 → 학생별 발행 → 미리보기 → 발행취소 → 이력 확인 흐름을 검증한다. 카테고리 통제 화면에서 AI 첨삭을 OFF로 변경한 뒤 학생 계정으로 AI 첨삭을 요청하는 시나리오도 포함한다.

### 11.3 화면 테스트

관리자 권한이 없는 사용자가 `/admin`, `/admin/certificates`, `/admin/categories`에 접근했을 때 차단되는지 확인한다. 데스크톱·모바일 화면에서 테이블, 모달, 탭, 토스트, 빈 상태가 모두 읽기 쉬운지 확인한다.

---

## 12. 단계적 구현 순서

| 단계 | 구현 범위 | 완료 기준 |
|---|---|---|
| 1단계 | 수료증 상태·관리자 ID·감사 필드 스키마 추가 | 마이그레이션 및 기존 데이터 호환 확인 |
| 2단계 | 관리자 수료증 목록·학생별 발행·취소 | 중복 방지와 권한 테스트 통과 |
| 3단계 | 기능 플래그 테이블과 관리자 카테고리 화면 | ON/OFF 상태가 새로고침 후에도 유지 |
| 4단계 | 학습 API에 기능 플래그 적용 | OFF 상태에서 서버 요청이 차단됨 |
| 5단계 | AI·교사 운영 지표 연결 | 영역별 대시보드에서 실제 DB 수치 표시 |
| 6단계 | 감사 로그·반응형·접근성·전체 테스트 | 운영 시나리오와 타입 체크 통과 |

---

## 13. 최종 운영 체크리스트

- [ ] 수료증 발행 시 학생·과정·레벨·유형 중복을 서버에서 검사한다.
- [ ] 발행취소와 삭제를 서로 다른 작업으로 구분한다.
- [ ] 발행취소 사유와 삭제 사유를 필수 또는 권장 정책으로 기록한다.
- [ ] 학생용 수료증 조회에서는 취소된 수료증을 유효한 증서로 노출하지 않는다.
- [ ] 카테고리 ON/OFF 값은 데이터베이스에 영속화한다.
- [ ] 카테고리 차단은 프론트엔드뿐 아니라 학습 API에서도 적용한다.
- [ ] 관리자 전용 API마다 서버 측 역할 검사를 둔다.
- [ ] 변경 이력에 관리자, 시각, 이전 값, 새 값, 사유를 기록한다.
- [ ] 수료증 발행·취소·삭제와 카테고리 변경을 Vitest로 검증한다.
- [ ] 모바일 화면과 Dialog 접근성 요구사항을 확인한다.

## 결론

이 설계안은 기존 `/admin` 대시보드를 단순 통계 화면이 아니라 **수료증·학습 카테고리·교사 첨삭·AI 학습도구를 실제로 운영하는 통합 관리자 콘솔**로 확장하는 기준을 제시한다. 가장 중요한 선행 작업은 수료증에 발행취소 상태와 감사 필드를 추가하고, 카테고리 통제 상태를 프론트엔드 임시 상태가 아닌 서버 저장형 기능 플래그로 전환하는 것이다. 이 두 기반이 마련되면 관리자 화면의 버튼이 실제 학습자 경험과 서버 동작을 일관되게 제어할 수 있다.

---

*작성 기준: 논술 마스터(Essay Master) 현재 관리자·수료증·커리큘럼·AI 학습도구 구조*  
*문서 작성자: Manus AI*  
*작성일: 2026-08-14*

---

## 부록 A. 권장 화면 와이어프레임 개요

```text
┌──────────────────────────────────────────────────────────────┐
│ 관리자 운영 콘솔     마지막 동기화 10:32     관리자 프로필 │
├───────────────┬──────────────────────────────────────────────┤
│ 운영 개요      │ [전체 발급] [이번 달 발급] [활성 카테고리]   │
│ 학생 관리      │ [AI 호출량] [미처리 첨삭]                    │
│ 수료증 관리    ├──────────────────────────────────────────────┤
│ 카테고리 통제  │ 수료증 관리                                  │
│ 교사 첨삭      │ 검색 [학생명/이메일] 과정 [전체] 상태 [전체] │
│ AI 학습도구    │                                              │
│ 감사 로그      │ 학생 | 과정 | 레벨 | 유형 | 상태 | 관리      │
│               │ 홍길동 | 초등 | L2 | 수료 | 활성 | ⋯        │
└───────────────┴──────────────────────────────────────────────┘
```

카테고리 통제 화면에서는 동일한 우측 콘텐츠 영역에 기능 카드 목록을 표시하고, 카드마다 활성화 스위치·적용 대상·최근 변경자·변경 이력을 보여준다. 이 구조는 운영자가 화면을 이동하지 않고도 현재 서비스가 어떤 상태로 학습자에게 제공되고 있는지 파악할 수 있게 한다.

## 부록 B. 작업 완료 메시지 예시

- 발행 성공: `홍길동 학생의 중고등 Level 2 수료증을 발행했습니다.`
- 발행취소 성공: `수료증을 발행취소했습니다. 취소 사유가 감사 로그에 기록되었습니다.`
- 기능 차단 성공: `AI 자동 첨삭을 차단했습니다. 학생의 신규 AI 요청은 서버에서 제한됩니다.`
- 권한 오류: `이 작업은 관리자 권한이 필요합니다.`
- 중복 오류: `동일 학생·과정·레벨의 활성 수료증이 이미 존재합니다.`

이 메시지는 관리자에게 결과와 영향 범위를 동시에 알려야 하며, 단순히 “저장되었습니다”라고만 표시하지 않는 것을 원칙으로 한다.

---

## 부록 C. 현재 구현과 후속 보완의 구분

현재 프로젝트에 이미 존재하는 관리자 대시보드·학생 상세·수료증 조회·AI 사용량 통계를 재사용할 수 있다. 반면 아래 항목은 설계에 따라 추가 구현이 필요한 영역으로 분류한다.

| 분류 | 기존 활용 가능 | 추가 보완 필요 |
|---|---|---|
| 관리자 접근 | 관리자 역할 기반 접근 | 작업별 세분 권한 |
| 수료증 목록 | 기본 수료증 조회 | 상태·취소 사유·감사 로그 |
| 수료증 발행 | 기존 발행 흐름 | 관리자 발행 사유·중복 서버 검사 |
| 카테고리 | 프론트 표시 구조 | DB 저장형 기능 플래그 |
| AI 관리 | 사용량 집계 | 기능 차단의 서버 적용·오류율 |
| 교사 관리 | 교사 피드백 페이지 | 교사 역할·배정·처리 현황 |
| 테스트 | 기존 인증·학습 테스트 | 수료증·기능 플래그 운영 시나리오 |

따라서 이 문서는 곧바로 코드를 대량 변경하기 위한 단순 화면 목록이 아니라, 데이터 모델·권한·운영 이력·학습자 영향까지 포함한 구현 기준서로 활용하는 것이 적절하다.
مت

---

**문서 끝**
