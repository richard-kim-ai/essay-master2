# 수료증·뱃지 QA 메모

2026-08-18 브라우저 검증에서 비인증 세션의 `/certificate` 접근은 로그인 안내 화면으로 제한되는 것을 확인했다. 샘플 과정 선택과 수료증 예시는 샘플 학생 계정으로 진입했을 때 검증해야 하며, 브라우저의 샘플 체험 버튼은 현재 세션에서 인증 상태를 변경하지 않아 별도 로그인 흐름 점검이 필요하다.

샘플 체험 버튼을 tRPC 인증 뮤테이션으로 교체한 뒤 요청이 `샘플 준비 중...` 상태에 머무는 현상을 재현했다. 응답 완료 여부와 인증 API 서버 로그를 다음 점검 단계에서 확인한다.

실제 로그인 사용자의 수료증 화면에서는 가입 과정인 일반/직장인 과정 카드만 표시되고 발급 목록도 해당 과정으로 제한되는 렌더링을 확인했다.

샘플 학생으로 로그인한 기본 마이페이지에서 과정 선택기가 나타났으며, 초등에서 고등/대입으로 변경하자 고등/대입 과정 표기로 즉시 전환되고 초등 뱃지·수료증 카운트는 모두 0으로 필터링되었다. 따라서 샘플 학생도 선택 과정에 맞는 성취 정보만 보게 된다.

고등/대입 샘플 상태에서 `학습 시작`을 누르면 고등/대입 커리큘럼의 3개 레슨으로 정상 이동했다. 수료증 화면도 고등/대입 수료증 예시 카드 1개와 해당 과정의 발급 목록만 표시했다.

초등 샘플 수료증의 `이미지 저장`을 실제 실행하자 "수료증 이미지·PDF 생성 중 오류가 발생했습니다" 토스트가 재현되었다. 내보내기 전용 복제 스타일 처리에서 발생한 오류를 추가 진단·수정해야 한다.

오류 원인이 CSS 색상 함수를 해석하지 못하는 DOM 캡처 계열 처리임을 확인하여, 현재는 화면 DOM을 캡처하지 않고 인증번호·수료자·과정·발급일만 독립 캔버스에 그려 PNG/PDF로 변환하도록 교체했다. 이 방식은 페이지 전체 인쇄를 방지하며 브라우저별 CSS 색상 해석 차이도 제거한다.

초등 샘플 수료증에서 수정된 `이미지 저장`을 다시 실행해 "수료증 원본 이미지(PNG)를 다운로드했습니다" 성공 알림을 확인했다.

`PDF 저장`도 "수료증 원본만 포함된 PDF를 저장했습니다" 성공 알림과 함께 파일로 생성되었고, 다운로드 PDF는 페이지 전체가 아닌 수료증 원본 1페이지로 렌더링되는 것을 확인했다.
